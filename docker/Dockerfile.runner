
FROM python:3.11-slim

WORKDIR /app

# Install basic dependencies
RUN pip install --no-cache-dir \
    requests \
    beautifulsoup4 \
    numpy \
    pandas

# Keep container alive
CMD ["tail", "-f", "/dev/null"]
